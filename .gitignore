node_modules/
npm-debug.log*

# Cloudflare Workers local dev
.wrangler/

# Datos locales / persistencia (no van al repo del add-on)
/data/

# Editores / SO
.DS_Store
Thumbs.db

# Worker MCP de Home Assistant: contiene la URL Nabu Casa de la instalacion.
# El repo es PUBLICO — esto no se sube nunca.
ha-mcp-worker/
